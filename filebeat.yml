filebeat.inputs:
  - type: filestream
    id: default-filestream
    paths:
      - filebeat_ingest_data/*.log


filebeat.autodiscover:
  providers:
    - type: docker
      hints.enabled: true


processors:
  - add_docker_metadata: ~


setup.kibana:
  host: ${KIBANA_HOSTS}
  username: ${ELASTIC_USER}
  password: ${ELASTIC_PASSWORD}


output.elasticsearch:
  index: "filebeat-%{+yyyy.MM.dd}"
  hosts: ${ELASTIC_HOSTS}
  username: ${ELASTIC_USER}
  password: ${ELASTIC_PASSWORD}
  ssl.enabled: true
  ssl.certificate_authorities: "/usr/share/filebeat/certs/ca/ca.crt"
  ssl:
    certificate: "/usr/share/filebeat/certs/es01/es01.crt"
    certificate_authorities: "/usr/share/filebeat/certs/ca/ca.crt"
    key: "/usr/share/filebeat/certs/es01/es01.key"
